name: 'Stencil Sass Production Release'

on:
  workflow_call:
    inputs:
      bump:
        description: 'Semver bump to apply (patch, minor, major).'
        required: true
        type: string
        default: patch

permissions:
  contents: read
  id-token: write

jobs:
  build_stencil_sass:
    name: 🏗️ Build Stencil Sass
    uses: ./.github/workflows/build.yml

  publish_production:
    name: 🚀 Publish Production Build
    needs: build_stencil_sass
    uses: ./.github/workflows/publish-npm.yml
    with:
      tag: latest
      version: ${{ inputs.bump }}
